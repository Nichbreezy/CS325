/*A Boolean operator which gives the value 1, if and only if all the operands are 1,
otherwise has a value of 0. Boolean operator which gives the value 1, if and only*/
 
class AND{
       static bool operation(bool A , bool B){
    return A && B;
       }
};