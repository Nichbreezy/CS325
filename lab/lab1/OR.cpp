/*OR operator is a Boolean operator that returns a value of TRUE if either (or both) of its operands is TRUE. */
#include<iostream>
#include<Gate.h>
using namespace std;
class OR{
    static bool()
    return 
}